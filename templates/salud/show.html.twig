{% extends 'base.html.twig' %}

{% block title %}Salud{% endblock %}

{% block body %}
    <h1>Salud</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salud.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('salud_index') }}">back to list</a>

    <a href="{{ path('salud_edit', {'id': salud.id}) }}">edit</a>

    {{ include('salud/_delete_form.html.twig') }}
{% endblock %}

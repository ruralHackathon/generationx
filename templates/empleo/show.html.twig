{% extends 'base.html.twig' %}

{% block title %}Empleo{% endblock %}

{% block body %}
    <h1>Empleo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ empleo.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('empleo_index') }}">back to list</a>

    <a href="{{ path('empleo_edit', {'id': empleo.id}) }}">edit</a>

    {{ include('empleo/_delete_form.html.twig') }}
{% endblock %}
